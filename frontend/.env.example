# Optional CARTO basemap key. Put the actual value in .env.local (ignored).
# Browser-visible basemap key only; never use an account secret here.
# Without a key, the map uses OpenStreetMap.
VITE_CARTO_API_KEY=
